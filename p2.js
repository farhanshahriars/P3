
/**
 * A function to check if the bear was dropped in the correct position
 * 
 * @param {*} ev event object loaded with drop event info
 * @author Basel
 */
function checkImage(ev) {
    ev.preventDefault();

    if (ev.target.id == 1 /*how Bhaumik decided to implement the words array*/ ) {
        /* JQUERY to insert the correct display into the html*/
    } else {
        /* JQUERY to insert the incorrect display into the html*/
    }
}